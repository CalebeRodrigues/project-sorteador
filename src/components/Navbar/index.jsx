import { Container, NavButton, Text } from './style';

export const Navbar = () => {
  return (
    <Container>
      <Text>Sorteador</Text>
      <NavButton>Como usar?</NavButton>
    </Container>
  );
};
